# Projekt pro plánování schůzek

Tento projekt slouží k plánování schůzek.

## Spuštění

1. Spusť `npm install`.
2. Spusť css:build z `package.json`.
3. Inicializuj db pomocí skriptu `init.sql`.
4. Pro přihlášení použij účet *testscio2@gmail.com*.